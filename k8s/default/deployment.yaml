apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-server-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-server
  template:
    metadata:
      labels:
        app: api-server
    spec:
      containers:
        - name: rocket-container
          image: us.gcr.io/cloud-shell-gcloud/rust_api_server:latest
          ports:
            - containerPort: 8000
